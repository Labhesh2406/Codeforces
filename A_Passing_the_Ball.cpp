#include <bits/stdc++.h>
using ll = long long;
using namespace std;

void solve() {
    ll n;
    string s;
    cin >> n >> s;
    int first_L = s.find('L');
    cout << (first_L + 1) << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--) {
        solve();
    }
    return 0;
}